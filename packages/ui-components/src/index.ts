export const operatorViewPanels = [
  "project-stack",
  "workspace",
  "context"
] as const;

export const focusViewPanels = ["workspace"] as const;

export const tabViewPanels = ["tabs", "workspace"] as const;

